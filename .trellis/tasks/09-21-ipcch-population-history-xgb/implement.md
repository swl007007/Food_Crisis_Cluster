# Execution plan — not yet authorized

Final-review proposal. Do not implement, fit, commit, start or close during this
planning turn. User will hand execution to Opus5 after the final spec review.

## 1. Establish the approved scope and audit baseline

- [ ] Read prd.md, technical-contract.md, feature-schema.json, candidate-configs.json,
  design.md and research/reuse-and-evidence.md fully. Confirm final user approval
  and separate execution instruction. Resolve any scientific conflict before code.
- [ ] Load trellis-before-dev and applicable backend specs. Inspect git state;
  other-session changes must not be reset, staged or silently incorporated.
- [ ] Check trellis-audit status. Prior open gates need accepted repair/re-audit;
  this task is ordinary research, not a remediation shortcut. Verify executor
  identity and exact registered lowercase repository path. Start controller only
  as part of authorized execution setup when needed.
- [ ] Commit only reviewed planning files with authorization; run GitNexus
  detect_changes before commit. Bound Opus session uses audit wrapper start to
  freeze that commit as base_sha. Verify active run and in_progress status.

## 2. Implement bounded preparation

- [ ] Create the new experiment folder and only the minimal modules in design.md.
  Pin source/lookup/release/runtime/helper hashes and exact inventories. Reuse
  existing pure APIs; before editing any existing symbol run GitNexus impact and
  report blast radius. Prefer no changes to existing experiments or core models.
- [ ] Reproduce target gate and original93; derive rich561 using the complete
  dated valid ledger. Save per-row history keys and formula/support audit.
- [ ] Construct unique keys, country mapping and complete fold/matched/full masks.
  Enforce feature origin and training-label cutoffs independently. No geometry run.
- [ ] Validate actual runtime, module paths, imputation behavior and finite values.

## 3. Implement development and finite selection

- [ ] Fit the six declared arms using explicit y/e/q3 targets, candidate inventories,
  matched/full keys, native NaNs or training-only RF fills and constant handling.
- [ ] Persist raw and crisis-oriented scores; implement bounded two-threshold
  joint F1 optimization, no-flip options and deterministic ties. No separate
  probability calibration, early stopping or additional candidate search.
- [ ] Implement the immutable freeze manifest: all configurations/thresholds and
  one primary family selected exclusively from2020-2022. Replay selection from
  stored predictions before final scoring is allowed.

## 4. Execute and verify the experiment

- [ ] Run focused contract tests, then real-source preparation/preflight. Run the
  first supported2020 h1 pilot, all six arms/configs, and log runtime/memory.
  Stop/report a resource barrier; do not shrink/change the approved science silently.
- [ ] Complete the bounded development schedule, reusing only exact pilot identity.
  Do not search feature subsets or inspect final scores to alter any choice.
- [ ] Freeze choices, then execute main schedule. Record absent test months,
  explicit constants, failed folds and all fitting keys/parameters. No-history
  rows use shared fullpool XGB .5 decisions; original experiments remain unchanged.
- [ ] Independently reconstruct candidate selection and final report. Refit the
  first/last nonempty main fold for each horizon (deduplicate if identical), all
  six selected methods, in a fresh evidence directory and verify predictions.
  Verification refits add at most48 fits, reported separately from5916 search/main.

## 5. Reporting and completion evidence

- [ ] Verify common keys and all full/matched/fallback routes. Recompute confusion,
  continuous errors, flip counts and prescribed country/year/cohort diagnostics.
- [ ] Replay shared-country bootstrap2000 draws, omitted-year checks and all
  conjunctive verdicts. Undefined required cells or failed evidence are incomplete.
  Report negative/mixed outcomes as valid scientific outcomes when evidence is complete.
- [ ] Build acceptance-evidence index mapping A1-A10 to files/commands/hashes.
  Commit audit-accessible per-row evidence, selection inputs and bootstrap draws;
  ignored local artifacts alone do not satisfy independent review.
- [ ] Load trellis-check, run appropriate package checks and GitNexus detect_changes.
  Commit completed implementation/evidence under user authorization. The bound
  executor calls trellis-audit close; do not native archive/finish as a substitute.
- [ ] Verify job ID/SHAs/accepted result. A launched audit is not a pass. Preserve
  incomplete findings and follow dedicated repair/re-audit workflow for gates.

## Planned commands (interfaces to implement)

Use the pinned Windows Python executable. Paths/source-root use Windows syntax
when passed to it; NEW_RUN denotes a fresh experiment-local root. Preparation
creates that root; later phases continue only its bound stage sequence.

```
python -B IPCCHPopulationHistoryExperiment/test_contracts.py
python -B IPCCHPopulationHistoryExperiment/prepare_data.py --source-root SOURCE_ROOT --run-dir NEW_RUN
python -B IPCCHPopulationHistoryExperiment/run_pipeline.py --run-dir NEW_RUN --stage pilot
python -B IPCCHPopulationHistoryExperiment/run_pipeline.py --run-dir NEW_RUN --stage development
python -B IPCCHPopulationHistoryExperiment/run_pipeline.py --run-dir NEW_RUN --stage main
python -B IPCCHPopulationHistoryExperiment/report_results.py --run-dir NEW_RUN --out-dir NEW_REPORT
```

One focused contract test module should test the actual boundaries: exact-.20
truth; phase5-only missing; sparse same-area histories and zeros; own-origin masks;
window edges and timestamp-based rates; ratio support; event versus missing ages;
RF training-only/all-missing/negative fills; XGB constant e/y; score orientation,
strict/inclusive cutoff equality and no-flip pairs; freeze causality and tie order;
common keys/fallback and joint draw multiplicities. Add a small independently
calculated fixture, not a suite that merely echoes constants. No root split-logic
test is needed unless that logic is actually edited; no such edit is planned.

## Exact audit repository identity

Registered root spelling (WSL alias casing matters to the controller):
`/mnt/c/users/swl00/ifpri dropbox/weilun shi/google fund/analysis/2.source_code/step5_geo_rf_trial/food_crisis_cluster`.

From the verified bound Claude/Opus session, after gates and approval:

```
trellis-audit --repo '<exact registered root above>' start ipcch-population-history-xgb
# Actual implementation, verification and authorized commit happen here.
trellis-audit --repo '<exact registered root above>' close
trellis-audit status
trellis-audit show JOB_ID
```

Do not spoof HERDR_PANE_ID, replace an active executor or assume an old session
still owns the pane. The user-level lifecycle instructions are in
/home/swl007007/.codex/AGENTS.md, referenced from ~/.claude/CLAUDE.md.
